# Django and REST Framework
Django>=4.2,<5.0
djangorestframework>=3.14.0
djangorestframework-simplejwt>=5.2.0
django-cors-headers>=4.0.0
django-filter>=23.0

# Database
psycopg2-binary>=2.9.0

# Production WSGI server (replaces the dev `runserver` in prod)
gunicorn>=21.2.0

# API Client
requests>=2.31.0
httpx>=0.24.0

# Google Sign-In ("Continue with Google") — verifies the id_token server-side
google-auth>=2.25.0

# Utilities
bcrypt>=4.0.0

# Development
django-extensions>=3.2.0
ipython>=8.12.0

# CSV Import/Export
django-import-export>=4.1.0
django-json-widget

# API Documentation
drf-spectacular>=0.26.0
drf-spectacular-sidecar>=2023.1.1

# Object storage (S3) — opt-in via USE_S3; acknowledgement media → S3 + CloudFront
django-storages>=1.14
boto3>=1.34.0